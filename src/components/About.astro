<section class="section about" id="about">
  <div class="container about-grid">
    <div data-reveal>
      <p class="eyebrow">About Dillz Bites</p>
      <h2 class="section-title">From sketch to final slice, your cake is built to match your moment.</h2>
      <p class="section-intro">
        Dillz Bites is a sole-proprietor bakery focused on custom cakes for meaningful celebrations. Every order is
        handmade in small batches with premium ingredients, thoughtful flavor pairing, and polished finishing details.
      </p>
      <p class="section-intro">
        We begin with your event goals, then shape design, portion size, and decoration style around your budget and
        timeline. The result is a cake that feels personal, memorable, and photo-ready.
      </p>
    </div>

    <div class="about-cards" data-reveal>
      <article>
        <h3>Design-first custom work</h3>
        <p>Each brief is tailored, with color direction and design motifs selected for your event.</p>
      </article>
      <article>
        <h3>Flavor and finish balance</h3>
        <p>Texture, sweetness, and serving practicality are tuned so the cake looks and tastes exceptional.</p>
      </article>
      <article>
        <h3>Clear communication</h3>
        <p>Orders are reviewed quickly with confirmation details shared directly by email.</p>
      </article>
    </div>
  </div>
</section>

<style>
  .about {
    background-color: var(--surface-muted);
  }

  .about-grid {
    display: grid;
    gap: var(--space-6);
    grid-template-columns: 1.1fr 0.9fr;
    align-items: start;
  }

  .about-cards {
    display: grid;
    gap: var(--space-4);
  }

  .about-cards article {
    background: var(--bg-surface);
    border: 1px solid var(--border-soft);
    border-radius: var(--radius-md);
    padding: var(--space-5);
    box-shadow: var(--shadow-sm);
  }

  .about-cards h3 {
    margin: 0;
    font-family: var(--font-display);
    font-size: 1.25rem;
  }

  .about-cards p {
    margin: var(--space-3) 0 0;
    color: var(--text-muted);
  }

  @media (max-width: 58rem) {
    .about-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
