<section class="section contact" id="contact">
  <div class="container">
    <p class="eyebrow" data-reveal>Contact</p>
    <h2 class="section-title" data-reveal>Tell us what you are celebrating and we will guide your next step.</h2>
    <p class="section-intro" data-reveal>
      For custom cakes, the fastest path is the order form. If you need quick clarification first, use the direct
      contact options below.
    </p>

    <div class="contact-grid" data-reveal>
      <article>
        <h3>Phone</h3>
        <p>Consultations and time-sensitive updates.</p>
        <a href="tel:+18687674628">(868) 767-4628</a>
      </article>

      <article>
        <h3>Email</h3>
        <p>General inquiries and order follow-up.</p>
        <a href="mailto:orders@dillzbites.com">orders@dillzbites.com</a>
      </article>

      <article>
        <h3>Hours</h3>
        <p>Tuesday-Saturday: 9:00 AM-6:00 PM</p>
        <p>Sunday: 10:00 AM-4:00 PM</p>
        <p>Closed Mondays</p>
      </article>
    </div>

    <div class="contact-actions" data-reveal>
      <a href="/order" class="button">Place a Cake Order</a>
      <a href="mailto:orders@dillzbites.com" class="button-outline">Send an Email</a>
    </div>
  </div>
</section>

<style>
  .contact-grid {
    margin-top: var(--space-6);
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: var(--space-4);
  }

  .contact-grid article {
    background: var(--bg-surface);
    border: 1px solid var(--border-soft);
    border-radius: var(--radius-md);
    padding: var(--space-5);
    box-shadow: var(--shadow-sm);
  }

  .contact-grid h3 {
    margin: 0;
    font-family: var(--font-display);
    font-size: 1.2rem;
  }

  .contact-grid p {
    margin: var(--space-2) 0;
    color: var(--text-muted);
  }

  .contact-grid a {
    color: var(--accent-primary);
    font-weight: 700;
    text-decoration: none;
  }

  .contact-actions {
    margin-top: var(--space-6);
    display: flex;
    gap: var(--space-3);
    flex-wrap: wrap;
  }

  @media (max-width: 52rem) {
    .contact-grid {
      grid-template-columns: 1fr;
    }
  }
</style>